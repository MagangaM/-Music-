# -Music-
Custom audio control interface built with HTML and JavaScript. It supports play, pause, and stop functions, along with a volume slider, mute option, playback speed control, and skip features. It also displays current playback time and duration, using the HTML5 Audio API without external libraries.
